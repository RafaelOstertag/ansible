---

- name: EC2 Web
  hosts: ec2_web
  become: yes

  tasks:
    - import_role:
        name: ec2/common/ssh-keys
      tags:
        - ssh
    - import_role:
        name: ec2/gateway/ipsec
      tags:
        - vpn
        - ipsec
        - tunnel
      become_user: root
