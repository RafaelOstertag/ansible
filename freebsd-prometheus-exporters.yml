---

- name: FreeBSD Prometheus Exporters
  user: root
  force_handlers: true
  hosts: prometheus_hosts
  tasks:
    - include_role:
        name: prometheus/node-exporter
      when: "ansible_distribution == 'FreeBSD'"
        
