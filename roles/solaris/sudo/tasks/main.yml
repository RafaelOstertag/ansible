---

- name: Copy sudoers file
  copy:
    src: sudo/sudoers
    dest: /etc/opt/ggl/sudoers
    mode: 0440
    owner: root
    group: root

- name: remove ldap.conf for sudo
  file:
    path: /etc/opt/ggl/ldap.sudo.conf
    state: absent
