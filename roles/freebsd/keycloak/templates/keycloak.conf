# Ansible maintained
db=postgres
db-username={{ postgresql_user }}
db-password={{ postgresql_password }}
db-url={{ postgresql_url }}
db-pool-max-size=10
hostname=sso.guengel.ch
http-relative-path=/auth
http-enabled=true
log=file
log-file=/var/log/keycloak/keycloak.log
proxy=edge
cache=local
