---

- name: FreeBSD Servers
  hosts: gizmo.kruemel.home
  force_handlers: true
  user: root
  tasks:
    - import_role:
        name: freebsd/rsync-server
      tags:
        - rsync
        - rsync-server
