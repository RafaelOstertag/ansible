#
# Maintained by ansible
#
fb{{ ansible_distribution_major_version }}-{{ ansible_machine }}-server: {
        URL: "http://colossus.kruemel.home/packages/fb{{ ansible_distribution_major_version }}-{{ ansible_machine }}-default-server",
        MIRROR_TYPE: "HTTP",
        ENABLED: YES
}
