---

- name: Prometheus Exporters
  user: root
  force_handlers: true
  hosts:
    - prometheus_hosts
    - ec2
  tasks:
    - import_role:
        name: prometheus/node-exporter
      when: "ansible_distribution == 'FreeBSD'"

