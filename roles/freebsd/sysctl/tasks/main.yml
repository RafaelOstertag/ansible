---

- name: Set {{ varname }} sysctl variable
  lineinfile:
    path: /etc/sysctl.conf
    create: true
    regexp: "^{{ varname }}"
    line: "{{ varname }}={{ value }}"
  notify:
    - FreeBSD reload sysctl
