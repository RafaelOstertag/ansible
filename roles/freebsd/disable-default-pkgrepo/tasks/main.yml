---

- name: create /usr/local/etc/pkg/repos
  file:
    dest: /usr/local/etc/pkg/repos
    state: directory
    mode: 0755
    owner: root
    group: wheel

- name: Disable FreeBSD pkg repo
  copy:
    src: FreeBSD.conf
    dest: /usr/local/etc/pkg/repos
    mode: 0644
