---

- name: FreeBSD DB Servers
  hosts: megatron.kruemel.home
  force_handlers: true
  user: root
  tasks:
    - import_role:
        name: db/postgresql-host
      tags:
        - db
        - postgres
