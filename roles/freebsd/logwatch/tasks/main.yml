---

- name: Remove logwatch
  pkgng:
    name: logwatch
    state: absent

- name: Remove logwatch configuration dir
  file:
    path: /usr/local/etc/logwatch
    state: absent

- name: Remove logwatch cron entry
  cron:
    name: Logwatch daily
    state: absent
