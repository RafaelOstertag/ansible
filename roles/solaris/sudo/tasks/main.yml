---

- name: Copy sudoers file
  file:
    src: sudo/sudoers
    dest: /etc/opt/ggl/sudoers
    mode: 0440
    owner: root
    group: root

- name: remove ldap.conf for sudo
  file:
    src: /etc/opt/ggl/ldap.sudo.conf
    state: absent
