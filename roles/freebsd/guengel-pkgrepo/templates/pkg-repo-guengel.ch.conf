# $Id$
#
# Maintained by ansible
#
{% if 'freebsd-server' in group_names %}
{%   set type = 'server' %}
{% else %}
{%   set type = 'client' %}
{% endif %}
fb{{ ansible_distribution_major_version }}-{{ ansible_machine }}-server: {
        URL: "pkg+http://pkg.kruemel.home/packages/fb{{ ansible_distribution_major_version }}-{{ ansible_machine }}-default-{{ type }}",
        MIRROR_TYPE: "SRV",
        ENABLED: YES
}
