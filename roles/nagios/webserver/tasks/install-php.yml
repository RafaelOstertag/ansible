---

- name: Install PHP for nagios
  pkgng:
    name: "{{ php_package }}"
    state: present
  loop:
    - mod_php81
    - php81-filter
  loop_control:
    loop_var: php_package
  notify:
    - Restart FreeBSD apache
