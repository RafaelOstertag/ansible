---

- name: FreeBSD Thin Jails
  hosts: thin_jail_servers
  force_handlers: true
  user: root
  tasks:
    - import_role:
        name: jails/host-setup
      tags:
        - host
        - host-setup

    - import_role:
        name: jails/thin-jail-setup
