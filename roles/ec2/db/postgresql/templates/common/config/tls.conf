# This file is maintained by ansible

ssl = on
ssl_ciphers = 'HIGH:!MEDIUM:!LOW:!aNULL'
ssl_prefer_server_ciphers = on
ssl_cert_file = '{{ pg_basedirectory }}/tls/server.crt'
ssl_key_file = '{{ pg_basedirectory }}/tls/server.key'
ssl_ca_file = '/etc/ssl/guengel.ch.pem'
