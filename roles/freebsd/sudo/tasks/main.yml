---

- name: Copy sudoers file
  file:
    src: sudo/sudoers
    dest: /usr/local/etc/sudoers
    mode: 0440
    owner: root
    group: wheel

- name: Remove sudoers from nsswitch
  lineinfile:
    path: /etc/nsswitch.conf
    regexp: "^sudoers:"
    state: absent

- name: Remove ldap.conf for sudo
  file:
    src: /usr/local/etc/ldap.conf
    state: absent
