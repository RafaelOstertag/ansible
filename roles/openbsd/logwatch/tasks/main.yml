---

- name: Remove logwatch configuration dir
  file:
    path: /etc/logwatch
    state: absent

- name: Remove logwatch
  file:
    path: /usr/local/share/logwatch
    state: absent

- name: Remove binary link
  file:
    path: /usr/sbin/logwatch
    state: absent
  
