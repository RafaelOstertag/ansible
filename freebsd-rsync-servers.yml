---

- name: FreeBSD rsync Servers
  hosts: gizmo.kruemel.home
  force_handlers: true
  user: root
  tasks:
    - import_role:
        name: rsync-server
      tags:
        - rsync
        - rsync-server
