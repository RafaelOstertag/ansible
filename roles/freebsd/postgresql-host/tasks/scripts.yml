---

- name: Copy scripts
  copy:
    src: "common/scripts/{{ item }}"
    dest: "/var/db/postgres/scripts/{{ item }}"
    mode: 0700
    owner: postgres
    group: postgres
  with_items:
    - backup